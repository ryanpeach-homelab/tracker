[project]
name = "tracker-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "sqlmodel>=0.0.21",
    "psycopg2-binary>=2.9",
]

[project.scripts]
tracker-mcp = "tracker_mcp.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
