# Placeholder version to satisfy the package build only. The running server's
# version is the TRACKER_VERSION environment variable baked into the container
# image at build time from the release tag (see get_version / tracker_mcp.version).
__version__ = "0.0.0"
